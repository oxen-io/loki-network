#!/usr/bin/env bash
#set -x
set +e
for file in "${SIGN_TARGET}/Resources/Frameworks/lokinet-extension.framework" "${SIGN_TARGET}/Contents/MacOS/Lokinet" ; do
    codesign -vvvv --force -s "${CODESIGN_KEY}" --entitlements "${SIGN_ENTITLEMENTS}" --deep --timestamp --options=runtime "$file" 
done
